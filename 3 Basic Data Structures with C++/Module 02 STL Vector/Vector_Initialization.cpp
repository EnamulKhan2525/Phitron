#include<bits/stdc++.h>
using namespace std;
int main()
{
    // vector<int>v;
    // vector<int>v(10);
    vector<int>v(10,25);
    for (int i = 0; i < v.size(); i++)
    {
        cout<<v[i]<<endl;
    }
    
    cout<<v.size();
    return 0;
}